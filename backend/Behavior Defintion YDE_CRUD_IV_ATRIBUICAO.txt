managed implementation in class ycl_de_crud_iv_atribuicao unique;
strict ( 2 );

define behavior for YDE_CRUD_IV_ATRIBUICAO //alias <alias_name>
persistent table ysatribuicao
lock master
authorization master ( instance )
early numbering
{
  create;
  update;
  //  delete;

  validation validarexistenciadados on save { create; field cnpj, cpf; }
  action deletaratribuicao result [1] $self;
  field ( readonly ) IdAtribuicao;
  mapping for ysatribuicao
  {
    IdAtribuicao = id_atribuicao;
    CNPJ = cnpj;
    Empresa = empresa;
    CPF = cpf;
    Nome = nome;
    DataInicio = data_inicio;
    DataFim = data_fim;
    Ativo = ativo;
    CriadoEm = criado_em;
    MudaEm = muda_em;
    Descricao = descricao;
  }
  determination setAtribuacaoAtivoDefault on modify { create; }
  determination preencherDescricao on modify { create; }

}